下面给你一套**可直接写进简历、又能实际落地**的评测方法，简洁、工程化、有数据，和你简历风格完全匹配。

---

## 一、检索质量指标怎么评测（Recall@k、Precision@k、MRR）
### 1. 准备测试集（必须写）
- 人工标注 **50–100条** 标准问答对：
  - 问题：用户真实问法（事实/对比/销售话术）
  - 标注正样本：**哪些文档块是正确答案来源**
- 示例：
  - Q：星辰ES9续航多少？
  - 正样本：产品手册「参数页」段落块ID=12、45

### 2. 计算 Recall@k（召回率）
**公式**：Recall@k = 检索结果前k条里「命中标注正样本」的数量 ÷ 所有标注正样本总数
- 常用：**Recall@5、Recall@10**
- 简历写法：
  - 构建 80 条标注测试集，Recall@5 从 65% 提升至 85%

### 3. 计算 Precision@k（精确率）
**公式**：Precision@k = 检索结果前k条里「命中标注正样本」的数量 ÷ k
- 简历写法：
  - Precision@5 稳定在 **≥80%**，有效过滤无关文档

### 4. 计算 MRR（平均倒数排名）
**公式**：对每条问题，取**第一个正样本在检索结果中的排名 r**，算 1/r；所有问题取平均
- 例：第一条正样本在第2位 → 1/2；第5位 → 1/5
- 简历写法：
  - MRR 从 0.62 提升至 **0.80**，首条正确结果更靠前

---

## 二、生成质量指标怎么评测（幻觉率、忠实度、相关度）
### 1. 幻觉率（核心指标）
- **人工评测**：随机抽 **100条** 生成答案，统计「编造不存在信息」的比例
- 简历写法：
  - 抽样100条对话，幻觉率从15%降至 **≤5%**

### 2. 答案忠实度（Faithfulness）
- **方法**：用 NLI（自然语言推理）模型（如 BGE、DeBERTa）判断「答案是否完全来自检索上下文，无额外编造」
- 简历写法：
  - 基于 NLI 自动校验，答案忠实度达 **≥90%**

### 3. 答案相关度（Relevance）
- **方法**：人工打分 1–5 分（1=完全不相关，5=精准回答），取平均分
- 简历写法：
  - 人工评测平均分 **≥4.2**，回答切题无冗余

---

## 三、端到端指标（响应耗时、并发、成功率）
- **检索耗时**：统计 **1000次** 检索请求，取 P95/P99
  - 简历：检索响应 P95 **≤200ms**
- **端到端耗时**：从用户输入到答案输出，统计 P95
  - 简历：端到端响应 P95 **≤800ms**
- **工具调用成功率**：成功调用次数 ÷ 总调用次数
  - 简历：工具调用成功率 **≥98%**
- **并发用户**：压测（如 Locust）稳定支持的用户数
  - 简历：支持 **100+并发用户** 稳定访问

