"""
LLM-as-a-Judge 评测提示词模板。

为汽车销售场景定制，涵盖忠实度、上下文相关性、
回答相关性、以及自动 QA 对生成等评测需求。
"""

# =========================================================================
# Faithfulness（忠实度/幻觉检测）
# =========================================================================

FAITHFULNESS_CLAIM_EXTRACTION = """你是一个专业的文本分析专家。请从以下生成的回答中提取所有独立的事实性陈述（claims）。

## 生成的回答
{answer}

## 要求
1. 将回答拆解为独立的事实陈述列表，每条陈述必须是可验证的事实主张
2. 忽略主观评价、寒暄语、销售话术中的修辞性表达
3. 每条陈述用一句话表达，不要包含多个事实
4. 如果回答中没有任何事实性陈述（如纯寒暄），返回空列表

请以 JSON 格式返回：
{{"claims": ["陈述1", "陈述2", ...]}}
"""

FAITHFULNESS_ENTAILMENT = """你是一个严格的事实核查专家。请判断以下陈述是否完全可以从提供的参考上下文中推断出来。

## 参考上下文（检索到的文档）
{context}

## 待核查陈述
{claim}

## 判断标准
- "supported": 陈述的所有信息都可以在上下文中找到，没有添加、扭曲或编造
- "contradicted": 陈述与上下文中的信息直接矛盾
- "unsupported": 陈述中的部分或全部信息在上下文中找不到依据（包括合理推断但原文未提及的情况）

请以 JSON 格式返回：
{{"verdict": "supported|contradicted|unsupported", "reason": "一句话说明判断依据"}}
"""

# =========================================================================
# Context Relevance（上下文相关性）
# =========================================================================

CONTEXT_RELEVANCE = """你是一个检索质量评估专家。请判断提供的文档片段是否与用户问题相关。

## 用户问题
{query}

## 文档片段
{chunk}

## 评判标准
- 相关：片段包含回答问题所需的信息，或者提供了有用的背景知识
- 不相关：片段内容与问题主题无关，或提供的信息对回答问题没有帮助

请以 JSON 格式返回：
{{"relevant": true/false, "reason": "一句话说明"}}
"""

# =========================================================================
# Answer Relevance（回答相关性）
# =========================================================================

ANSWER_RELEVANCE = """你是一个回答质量评估专家。请评估以下回答与用户问题的相关程度。

## 用户问题
{query}

## 生成的回答
{answer}

## 评估维度
1. 回答是否直接回应了用户的问题？
2. 回答是否包含了问题相关的关键信息？
3. 回答是否偏离了主题或包含了无关内容？

请给出 1-5 的评分（1=完全不相关，5=完美回应），并说明理由。

请以 JSON 格式返回：
{{"score": 1-5, "reason": "简要说明评分依据"}}
"""

# =========================================================================
# QA Pair Generation（评测数据集自动生成）
# =========================================================================

QA_PAIR_GENERATION = """你是一位汽车销售培训专家，同时也是一个高质量评测数据生成器。
请基于提供的文档片段，生成可用于 RAG 系统评测的问答对。

## 文档片段
{chunk}

## 生成要求
1. 生成 {num_pairs} 个不同类型的问答对：
   - **事实查询型**：询问具体的参数、配置、价格等（如"XX车型的续航里程是多少？"）
   - **对比分析型**：需要综合多个信息点进行回答（如"XX车型和同级别车型相比有哪些优势？"）
   - **销售场景型**：模拟真实客户会问的问题（如"这款车适合家庭使用吗？"）
2. 问题必须基于文档片段中的真实信息
3. 答案（ground_truth）必须准确、完整，且可以仅从该文档片段中得出
4. 标注出回答所需的关键信息点（key_points）

请以 JSON 格式返回（返回一个数组）：
[
  {{
    "query": "问题文本",
    "ground_truth": "标准答案",
    "type": "factual|comparative|sales_scenario",
    "difficulty": "easy|medium|hard",
    "key_points": ["回答必须包含的关键信息点"]
  }}
]
"""

# =========================================================================
# Dataset Quality Review（数据集质量审核）
# =========================================================================

DATASET_QUALITY_REVIEW = """你是一个数据质量审核专家。请审查以下评测数据项的质量。

## 评测数据项
查询: {query}
标准答案: {ground_truth}
相关性文档: {relevant_chunks}

## 审核标准
1. 查询是否清晰、自然（像真实用户的提问）？
2. 标准答案是否准确、完整？
3. 标准答案是否可以从相关性文档中得出？
4. 难度标注是否合理？

请以 JSON 格式返回：
{{
  "pass": true/false,
  "issues": ["问题描述（如有）"],
  "suggested_fix": "修改建议（如有）"
}}
"""
